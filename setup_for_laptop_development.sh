#!/bin/bash

# -e tells bash to exit immediately on error, instead of its default of continuing
# -x prints out each command being run for easier debugging
# set -e

echo "setup for laptop development"
